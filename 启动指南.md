# CodeHub 项目启动指南

## 环境要求

- JDK 21（路径：E:/jdk-21.0.7+6）
- Maven（IDEA自带）
- Node.js 20+ / npm 10+
- Docker Desktop

---

## 方式一：Docker Compose 一键部署（推荐）

```bash
cd F:/work/CodeHub

# 设置LLM API Key（可选，不设置则AI问答不可用）
export LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
export LLM_API_KEY=你的key
export LLM_MODEL=qwen3.7-plus

# 一键启动所有服务
docker-compose up -d --build
```

访问 http://localhost 即可使用。

包含 5 个服务：
| 服务 | 端口 | 说明 |
|------|------|------|
| frontend | 80 | Nginx托管前端+反向代理后端 |
| backend | 8080 | Spring Boot后端API |
| mysql | 3307 | MySQL数据库 |
| redis | 6379 | Redis缓存 |
| minio | 9000/9001 | MinIO对象存储 |

```bash
# 查看日志
docker-compose logs -f backend

# 停止所有服务
docker-compose down

# 停止并删除数据（慎用）
docker-compose down -v
```

---

## 方式二：本地开发模式

### 第1步：启动Docker基础设施

```bash
cd F:/work/CodeHub/codehub-server
docker-compose up -d
```

验证：
```bash
docker ps --filter "name=codehub"
# 应该看到3个容器：
# codehub-mysql   端口3307
# codehub-redis   端口6379
# codehub-minio   端口9000 + 9001
```

### 第2步：启动后端

```bash
cd F:/work/CodeHub/codehub-server
set JAVA_HOME=E:/jdk-21.0.7+6
set LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
set LLM_API_KEY=你的key
set LLM_MODEL=qwen3.7-plus
mvn spring-boot:run
```

看到 `Started CodeHubApplication in X seconds` 启动成功。

后端地址：http://localhost:8080

### 第3步：启动前端

```bash
cd F:/work/CodeHub/codehub-web
npm run dev
```

看到 `Local: http://localhost:5173/` 启动成功。

### 第4步：打开浏览器

访问 http://localhost:5173

测试账号：
- testuser / 123456（管理员）
- demo / 123456（普通用户）

---

## 关闭项目

```bash
# 关前端：终端按 Ctrl+C
# 关后端：终端按 Ctrl+C
# 关Docker：
docker-compose down
```

---

## 常见问题

### 端口被占用

```bash
# 查找占用端口的进程
netstat -ano | findstr ":8080"
netstat -ano | findstr ":5173"
netstat -ano | findstr ":3307"

# 杀掉进程（PID换成实际数字）
taskkill /F /PID <PID>
```

### JDK版本问题

后端必须用 JDK 21 编译运行（Lombok 不兼容 JDK 25）。

```bash
set JAVA_HOME=E:/jdk-21.0.7+6
```

### Docker容器起不来

```bash
# 查看日志
docker logs codehub-mysql
docker logs codehub-redis
docker logs codehub-minio

# 重建容器（数据会丢失）
docker-compose down -v
docker-compose up -d
```

### 前端页面白屏

浏览器按F12打开控制台，看红色报错。常见原因：
1. localStorage有脏数据 → 控制台执行 `localStorage.clear()` 刷新
2. 后端没启动 → 前端代理转发失败
3. 端口不对 → 确认后端8080、前端5173

### npm install失败

```bash
# 清掉node_modules重装
cd F:/work/CodeHub/codehub-web
rm -rf node_modules package-lock.json
npm install
```

---

## 项目结构

```
CodeHub/
├── docker-compose.yml         # 全栈Docker编排（5个服务）
├── nginx.conf                 # Nginx配置（前端+反向代理）
├── codehub-server/            # 后端（Spring Boot 3.2）
│   ├── Dockerfile             # 后端镜像构建
│   ├── pom.xml
│   ├── sql/init.sql           # 建表SQL
│   └── src/main/java/com/codehub/
│
└── codehub-web/               # 前端（Vue3 + TypeScript）
    ├── Dockerfile             # 前端镜像构建
    ├── nginx.conf             # Nginx配置（复制用）
    ├── package.json
    └── src/
```

## 技术栈

| 组件 | 技术 | 端口 |
|------|------|------|
| 后端 | Spring Boot 3.2 + MyBatis-Plus | 8080 |
| 前端 | Vue3 + TypeScript + Element Plus | 5173/80 |
| 数据库 | MySQL 8.0 | 3307 |
| 缓存 | Redis 7 | 6379 |
| 对象存储 | MinIO | 9000/9001 |
| AI模型 | 阿里云百炼 qwen3.7-plus | - |
| API文档 | Knife4j | 8080/doc.html |
